function draw_debug()
    -- This function can be used for anything we need urgently to display to the screen,
    -- so it might change a lot!

    -- set_color(1)
    -- love.graphics.print("Accuracy Gen: %s" % game.state.accuracygen, 200,108)
    -- reset_color()
end



function test_load_in()
    play_music("birdcage_bounce")
end